export const metadata = {
    title: 'Project Management',
    description: 'Home Page of Project Management',
}

export default function SiteLayout({ children }) {
    return (
        <main>{children}</main>
    )
}
